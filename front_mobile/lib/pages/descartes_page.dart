import 'package:flutter/material.dart';

class DescartesPage extends StatelessWidget {
  const DescartesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Página de descartes')),
    );
  }
}
