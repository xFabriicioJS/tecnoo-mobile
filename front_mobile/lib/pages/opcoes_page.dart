import 'package:flutter/material.dart';

class OpcoesPage extends StatelessWidget {
  const OpcoesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Página de opções')),
    );
  }
}
